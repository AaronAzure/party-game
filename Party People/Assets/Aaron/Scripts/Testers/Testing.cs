using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Rewired;
using UnityEngine.EventSystems;

public class Testing : MonoBehaviour
{
    [SerializeField] private int nPlayers;
    // [SerializeField] private 

    void Start()
    {
        int[] np = new int[50];
        foreach (int heas in np)
        {
            
        }
    }
}
